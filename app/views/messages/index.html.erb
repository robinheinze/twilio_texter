<h1>There are messages here, we swear!</h1>

<% @messages.each do |message| %>
  <p><%= link_to message.id, message_path(message) %></p>
<% end %>

<%= link_to "New Message", new_message_path, :class => 'btn btn-danger' %>
